diff --git a/api/openapi.yaml b/api/openapi.yaml
index 1234567..89abcde 100644
--- a/api/openapi.yaml
+++ b/api/openapi.yaml
@@ -10,18 +10,21 @@ paths:
       parameters:
         - name: limit
           in: query
-          schema: { type: integer, default: 50, maximum: 1000 }
+          schema: { type: integer, default: 20, maximum: 100 }
         - name: cursor
           in: query
+          required: true
           schema: { type: string }
       responses:
-        '200':
+        '204':
           description: list of orders
           content:
             application/json:
               schema:
                 type: object
                 properties:
                   items:
                     type: array
                     items: { $ref: '#/components/schemas/Order' }
+                  # legacy 'next' field removed; clients must use Link header
@@ -45,7 +48,6 @@ components:
       type: object
       required: [id, customer_id, total]
       properties:
         id: { type: string }
-        customer_id: { type: string }
         customer:
           $ref: '#/components/schemas/Customer'
         total: { type: number }
diff --git a/api/migrations/2026_05_13_add_currency.sql b/api/migrations/2026_05_13_add_currency.sql
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/api/migrations/2026_05_13_add_currency.sql
@@ -0,0 +1,3 @@
+ALTER TABLE orders
+  ADD COLUMN currency VARCHAR(3) NOT NULL;
+-- no backfill statement, no DEFAULT clause
